#ifndef KEYBOARD_H
#define KEYBOARD_H
#include <QKeyEvent>
#include <QGraphicsView>
#include <QPushButton>
#include <tuple>


#include "chip8.h"
using namespace std;

namespace Keyboard {
/*void execute_0NNN();

void on_key_pressed(QKeyEvent* event);
void on_key_pressed(QPushButton *button, CHIP8 *chip8);
void on_key_released(QKeyEvent* event);
void on_key_released(QPushButton *button, CHIP8 *chip8);

//void on_key_pressed(QKeyEvent* event);
//void keyReleaseEvent(QKeyEvent* event) override;
//on key pressed; on key released; on key clicked;
//std::tuple<CHIP8::KeyStrokes, QPushButton *ui_button> find_key(QKeyEvent *event);
void apply_key_action(CHIP8::KeyStrokes key, bool is_pressed);

void toggle_displayed_key(QPushButton *button, bool is_key_pressed);
void toggle_key(QPushButton *button, bool is_key_pressed)*/
}

#endif // KEYBOARD_H
